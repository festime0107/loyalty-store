generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"      // mund ta ndryshosh në "postgresql", "mysql" etj.
  url      = env("DATABASE_URL")
}

model Customer {
  id         String     @id @default(uuid())
  name       String
  birthdate  DateTime
  email      String     @unique
  cardNumber String     @unique
  purchases  Purchase[]
}

model Purchase {
  id         String   @id @default(uuid())
  date       DateTime @default(now())
  product    String
  customer   Customer @relation(fields: [customerId], references: [id])
  customerId String
}
